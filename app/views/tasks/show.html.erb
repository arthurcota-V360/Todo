<p style="color: green"><%= notice %></p>

<h1><%= @task.title %></h1>
<p><%= @task.description %>


<div>
  <%= link_to "Edit this task", edit_task_path(@task), class: "btn btn-primary" %> |
  <%= link_to "Back to tasks", tasks_path, class: "btn btn-secondary" %>

  <%= button_to "Destroy this task", @task, method: :delete, class: "btn btn-danger", :data => {:confirm => 'Are you sure?'} %>
</div>